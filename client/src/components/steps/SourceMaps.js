import React, { Fragment } from "react";
import { List } from "../Components";

export default () => (
  <Fragment>
    <h1>Source</h1>
    <p>
      When debugging, always use source code <br />
      For that, we have sourcemaps
    </p>
  </Fragment>
);
